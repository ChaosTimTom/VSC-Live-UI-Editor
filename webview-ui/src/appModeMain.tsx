import React from 'react';
import ReactDOM from 'react-dom/client';
import AppModeShell from './appMode/AppModeShell';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <AppModeShell />
  </React.StrictMode>
);
